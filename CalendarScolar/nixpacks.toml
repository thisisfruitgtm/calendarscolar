[phases.build]
cmds = [
  'npx prisma generate',
  'DATABASE_URL=file:./prisma/build.db npx prisma db push --skip-generate',
  'DATABASE_URL=file:./prisma/build.db npm run build',
  'rm -f prisma/build.db',
]

[start]
cmd = 'bash start.sh'
